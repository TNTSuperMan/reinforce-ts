{
  "name": "reinforcejs",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build-recurrent": "bun build --minify --outdir=dist --entry-naming=recurrent.js recurrent/index.ts"
  }
}
